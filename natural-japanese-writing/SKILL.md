---
name: natural-japanese-writing
description: >
  Generates and reviews Japanese text with natural, concise language by
  eliminating AI-specific writing patterns.
  Use when writing or reviewing Japanese reports, documents, or any text output
  that must read as natural, human-written Japanese.
  Japanese triggers: レポート、報告書、文書、まとめ、サマリー、日本語で書く、日本語で出力、自然な日本語
---

# 自然な日本語の記述

AI特有の文章スタイルを排除し、簡潔で読みやすい日本語テキストを生成・レビューする。

## クイックスタート

1. 文書の種類と対象読者を指定する
2. 文体を指定する：`です・ます調` または `だ・である調`
3. テーマ・アウトライン・既存の下書きを提供する
4. テキストを生成または修正する

## 文体の選択

呼び出し時に文体が指定された場合は必ずそれに従う。指定がない場合は文書の種類から推定する。

| 文書の種類 | デフォルトの文体 |
|---|---|
| 対外レポート・クライアント向け文書 | `です・ます調` |
| 社内技術レポート・調査文書 | `だ・である調` |

## 記述ルール

**文の長さ**
- 意味を持たない語は省く。
- 長い文章になる場合は、適切な読点・句点を織り交ぜ、改行して読みやすさを確保する。

**語彙**
- 格式張った表現より平易な言葉を優先する。
  - ✗ `鑑みると` → ✓ `考えると`
  - ✗ `実施する` → ✓ `行う`

**助詞**
- `は`・`が`・`を`・`に` などの助詞は必ず記述する。簡潔さのために省略しない。

**略語**
- 括弧書きで正式名称を補足しない。略語をそのまま使用する。
  - ✗ `AWS（Amazon Web Services）` → ✓ `AWS`

## 排除すべきパターン

| パターン | 例 | 修正方法 |
|---|---|---|
| 冗長な言い回し | `〜という点において` | `〜において` に短縮、または文を再構成する |
| 水増し表現 | `〜することができます` | `〜できます` に置き換える |
| 形式的な接続詞の乱用 | `また、` `さらに、` `なお、` の機械的な多用 | 意味上必要な場合のみ使用する |
| 内容の繰り返し | 段落の冒頭と末尾で同じ内容を述べる | 一度だけ記述する |
| 過剰な箇条書き | 散文で自然に読める内容をリスト化する | 散文で記述する |

## 図解

- 図解にアスキーアートを使用しない。
- すべての図解にMermaidを使用する。ダイアグラムの種類はユーザーの指示に従う。
- 種類の指定がない場合は、内容から最適なMermaidのダイアグラムタイプを推定する。

## 注意事項

- 既存のテキストを修正する場合、元の意味を保持する。新しい情報を追加しない
